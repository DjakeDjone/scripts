# Power Scripts Linux (Gnome)

## ScreenshotOCR

Takes a screenshot and extracts text from it using OCR.

## Translation

Translates text using Google Translate.

dependencies:

```bash
sudo apt-get update
sudo apt-get install translate-shell libnotify-bin xclip
```
